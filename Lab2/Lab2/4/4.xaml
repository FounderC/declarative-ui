<Window x:Class="Lab2._4._4"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TextEditor" Height="400" Width="600">

    <Window.Resources>
        <LinearGradientBrush x:Key="LinearPanelBrush"
                             StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="Yellow" Offset="0" />
            <GradientStop Color="LightBlue" Offset="0.5" />
            <GradientStop Color="Yellow" Offset="1" />
        </LinearGradientBrush>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                BorderBrush="#FFCBBEBE"
                BorderThickness="2"
                CornerRadius="10"
                Margin="5"
                Background="{StaticResource LinearPanelBrush}">
            <StackPanel Orientation="Horizontal" Margin="5">
                <Button ToolTip="Відкрити">
                    <Image Source="images/open.png" Width="32" Height="32"/>
                </Button>
                <Button ToolTip="Очистити">
                    <Image Source="images/clear.png" Width="32" Height="32"/>
                </Button>
                <Button ToolTip="Копіювати">
                    <Image Source="images/copy.png" Width="32" Height="32"/>
                </Button>
                <Button ToolTip="Вставити">
                    <Image Source="images/paste.png" Width="32" Height="32"/>
                </Button>
            </StackPanel>
        </Border>

        <Border
                BorderBrush="#FFCBBEBE"
                BorderThickness="2"
                CornerRadius="10"
                Margin="5,56,5,5"
                Background="{StaticResource LinearPanelBrush}" Grid.RowSpan="2">
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBox Grid.Row="0"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         TextWrapping="Wrap"
                         FontSize="{Binding ElementName=fontSlider, Path=Value, UpdateSourceTrigger=PropertyChanged}"
                         Margin="5" TextChanged="TextBox_TextChanged_1">
                    TEXT
                </TextBox>

                <DockPanel Grid.Row="1" LastChildFill="False">
                    <Label Content="Розмір шрифту:"
                           DockPanel.Dock="Left"
                           Margin="5"/>
                    <Slider x:Name="fontSlider"
                            DockPanel.Dock="Left"
                            Margin="4"
                            MinWidth="100"
                            Minimum="12"
                            Maximum="100"
                            Value="24"/>
                    <Button Content="OK"
                            DockPanel.Dock="Right"
                            Margin="5"
                            Style="{x:Null}"/>
                </DockPanel>
            </Grid>
        </Border>
    </Grid>
</Window>

